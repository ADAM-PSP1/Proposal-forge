[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[functions."claude"]
  timeout = 60

[[redirects]]
  from = "/api/claude"
  to = "/.netlify/functions/claude"
  status = 200
